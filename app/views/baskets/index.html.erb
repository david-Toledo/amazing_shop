<h1>Baskets#index</h1>
<p>Find me in app/views/baskets/index.html.erb</p>

<ul>

  <% @baskets.each do |basket| %>
  <li>
    <%= link_to basket.user_id, basket_path(basket.id) %>
  </li>
  <% end %>
</ul>
